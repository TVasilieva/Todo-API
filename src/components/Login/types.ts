export default interface ComponentProps {}

export type LoginHeader = {
  id: string;
  className: string;
  name: string;
};

export type SignUp = {
  htmlFor: string;
  name: string;
  type: string;
  "data-type"?: string;
};
